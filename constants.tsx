import type { EquipmentLevels } from './types';

export const TILE_ROWS = 500;
export const TILE_COLS = 12;
export const TILE_SIZE_PX = 64;

export const initialEquipment: EquipmentLevels = {
  WEAPON: 0,
  SHIELD: 0,
  HELMET: 0,
  ARMOR: 0,
  LEGS: 0,
  BOOTS: 0,
};
